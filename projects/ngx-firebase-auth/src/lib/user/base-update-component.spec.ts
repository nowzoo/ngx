import { BaseUpdateComponent } from './base-update-component';

describe('BaseUpdateComponent', () => {
  it('should create an instance', () => {
    expect(new BaseUpdateComponent()).toBeTruthy();
  });
});
